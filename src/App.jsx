import Contador from './Contador'

const InstagramApp = () => {
    return(
        <div>
            <h1>Componente InstagramApp</h1>
            <Contador />
        </div>
    )
}

export default InstagramApp;